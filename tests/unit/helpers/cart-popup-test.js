import { cartPopup } from 'question-answer/helpers/cart-popup';
import { module, test } from 'qunit';

module('Unit | Helper | cart popup');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartPopup([42]);
  assert.ok(result);
});
